1. Derive `EndpointHandler` from the upstream contract rather than restating it, in the `src/core/types.ts` file. `@orkestrel/tool` exports the options type whose `execute` member this mirrors, so taking that member structurally makes the next upstream parameter change land silently and correctly. Hand-restating it is what produced the typecheck failure that forced the 0.0.14 adoption, across one source call site and every direct call in the factories test.
2. Rule on the execution context every other tool this package builds now receives and ignores: the workflow tool, the workspace tool, the agent tool, the database tool, the relation tool, the prompt tool, and the answer tool. The database tool is the sharpest case, because its guide documents it passing a fresh abort signal of its own to table operations while the caller's signal sits unread in the context beside it. Either forward the caller's signal or state on each handler why it does not, and pin whichever the answer is.
3. Name the endpoint coverage in the `tests/src/core/factories.test.ts` bullet of the `guides/toolbox.md` guide. The three forwarding proofs the 0.0.14 release added have no narrative home there, and the parity gate does not reach a bullet's prose.
